{
  "name": "@tournamental-plugin/example-hello-odds",
  "version": "0.0.1",
  "description": "Minimal oddsSource plugin example. Returns synthetic implied probabilities from a deterministic hash of matchId. The thinnest possible reference for the oddsSource extension point.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": ["src", "plugin.json", "README.md"],
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@tournamental/plugin-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "typescript": "^5.6.3",
    "vitest": "^4.1.8"
  },
  "keywords": ["tournamental-plugin", "oddsSource", "example"]
}
