{
  "name": "@vtorn/web",
  "version": "0.0.0",
  "private": true,
  "description": "VTorn 3D match renderer (Next.js + React Three Fiber)",
  "dependencies": {
    "@vtorn/spec": "workspace:*"
  }
}
