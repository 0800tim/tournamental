{
  "name": "@vtorn/security-watchdog",
  "version": "0.1.0",
  "private": true,
  "description": "Tournamental security watchdog — daily/weekly scans, severity-routed alerts, finding lifecycle (ack/resolve), audit log.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "vtorn-watchdog": "dist/cli.js"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "prestart": "tsc -p tsconfig.json",
    "scan": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: noop (typecheck covers it)'"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/sensible": "^6.0.1",
    "fastify": "^5.0.0",
    "pino": "^9.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
